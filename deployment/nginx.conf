server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback — client-side React Router owns all non-asset paths.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
